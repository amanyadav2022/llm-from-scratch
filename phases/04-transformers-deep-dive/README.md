# Phase 4 — Transformers Deep Dive

*The most important phase in the roadmap. Split into three parts so each stays focused.*

1. [Tokenization, Embeddings, Positional Encoding](01-tokenization-embeddings-positional.md) — getting text into the model
2. [Self-Attention, Multi-Head Attention, Causal Masking](02-self-attention.md) — the core mechanism itself
3. [The Full Transformer Block](03-full-transformer-block.md) — feedforward network, residual connections, layer norm — assembling the repeatable unit that gets stacked into a full model

Read them in order. Each part assumes the one before it.
